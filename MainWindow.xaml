<Window x:Class="MusicPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vlc="clr-namespace:Vlc.DotNet.Wpf;assembly=Vlc.DotNet.Wpf"
        xmlns:local="clr-namespace:MusicPlayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TreeView x:Name="treeView" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Nodes}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock IsEnabled="False" Text="{Binding Icon}" TextWrapping="Wrap"/>
                        <TextBlock IsEnabled="False" Text="{Binding Name}" TextWrapping="Wrap"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        
        <!--C:\Users\ne_hochu\Desktop\Запрещённые Барабанщики - Убили Негра.mp3-->
        <MediaElement x:Name="MediaPlayer" Visibility="Collapsed"/>
        <ListView x:Name="ViewTracks" Grid.RowSpan="2" Grid.ColumnSpan="3" 
                  Grid.Row="0" Grid.Column="1" />

        <Grid Grid.Row="2" Grid.ColumnSpan="3" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.3*"/>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <MediaElement x:Name="playerElement" MediaOpened="PlayerElementSourceUpdated" />
            <Slider x:Name="playerSlider" Grid.Row="0" Grid.ColumnSpan="3" ValueChanged="SliderValueChanged" />
            
            <!-- Maximum="{Binding ElementName=playerElement, Path=Position.TotalSeconds}" -->
            <Button Content="⏮️" FontSize="35" Grid.Column="0" Grid.Row="2" />
            <Button Content="▶️" x:Name="playButton" FontSize="50" HorizontalAlignment="Center" 
                    VerticalAlignment="Bottom" Width="100" Height="100" Grid.Column="1" 
                    Grid.Row="1"  Grid.RowSpan="2" Click="PlayPauseClick">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="50"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="⏭️" FontSize="35" Grid.Column="2" Grid.Row="2" />

        </Grid>

        <!--
        <Button Content="&lt;" Grid.Column="1" Grid.Row="2" />
        <Button Content="||" Grid.Column="2" Grid.Row="2" />
        <Button Content="&gt;" Grid.Column="3" Grid.Row="2" />
        -->
    </Grid>
</Window>
